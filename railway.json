{
  "build": {
    "base": "backend",
    "backend": {
      "builder": "NIXPACKS",
      "buildCommand": "cd backend && npm install"
    }
  },
  "deploy": {
    "backend": {
      "startCommand": "cd backend && node server.js"
    }
  }
}
